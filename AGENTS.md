# AGENTS.md — 名实项目执行要求（LLMs 与贡献者通用）

本文件是「名实」仓库的判断标准与执行规范。任何 LLM 或人类贡献者修改本仓库前，必须阅读并遵守本文件。

## 1. 项目一句话

考辨译名：把被音译吞掉的语义，用意译还回来。名不正则言不顺，译不正则义不明。

## 2. 判断标准（判据三问）

对任何一个候选词条，先回答三问：

1. **长不长？**——音译字符是否冗长、拗口（如"基孔肯雅热"五字注音）？
2. **有没有价值？**——所选汉字是否贡献了任何语义（如"特斯拉""布尔"仅记音）？
3. **有没有中文含义？**——字面能否独立成义（如"泵"= 石 + 水，水击石而喷出）？

三问全中 → 垃圾翻译（懒译），应收录进懒译表并给出理想意译。
三问全不中 → 高价值翻译（如单字造字"泵"），应收录进巧译或典范表。

第 3 问以**现代普通读者的视角**为准：依赖通假字、生僻典故的"意译"（如"句柄"= 句通勾 + 柄）读者读不出来，视同无义——意译的可读性由读者决定，不由译者本意决定。

**通用词例外**：已全民通用的音译词（艾滋病、阿司匹林、布洛芬、尼古丁）不再按懒译批判，直接归入 09 固化音译——通用即免罪，评级最低。

## 3. 分类体系与归属文件

| 类别 | 判据 | 归属文件 |
|---|---|---|
| 纯音译（懒译） | 只取音、字面无义，且有意译替代可能 | 01-医学 / 02-数学 / 03-物理 / 04-化学 / 05-生物学 / 06-工程与通用（按学科） |
| 编号化丢义 | 非音译，用编号/序号命名，不承载语义（如甲肝乙肝） | 同上（按学科），备注注明"编号化" |
| 人名综合征/体征（Eponyms） | 人名音译、字面无义（如麦氏点、巴氏征） | 01-医学.md「人名综合征与体征」小节 |
| 音译兼意译（巧译） | 取音 + 所选汉字自带贴切含义 | 08-音译兼意译.md |
| 音译造字（再造） | 取音 + 创造新汉字，字形自带含义 | 08-音译兼意译.md（偏意译者入 07） |
| 纯意译（典范） | 完全意译且译得漂亮 | 07-意译典范.md |
| 固化音译（已成词） | 全民通用、翻案无望（如沙发、雷达、艾滋病、阿司匹林） | 09-固化音译.md |

**判断标准与时代无关**：新造词同样适用。人名、地名、商标名不豁免。

**优先级**：医学人名综合征/体征（Eponyms）是本项目的核心批判对象，优先收录、优先考证——用无义人名命名体征，是医学领域最典型的懒译。

## 4. 词条格式规范

学科懒译表（01-06）字段：`原始中文 | 原始来源语言 | 正确译文 | 备注`

- 原始来源语言：写明语言 + 人名/地名/商标/缩写等来源，如"英语（人名 McBurney）"
- 正确译文（现代推荐译名）：历史真实意译名优先，无历史译名时可新拟（如句柄 → 操控符）
- 备注：限一句话（可用分号），说明词源或为何意译更佳；**不得轻称"意译"**——词级对译才可称意译，独立概念造词（如"激素"之于"荷尔蒙"）须注明"独立造词、同实异名"
- 每条收录前必须考证词源（web 检索），**禁止编造**

对照表字段：

- 巧译表（08）：`中文译名 | 原词 / 来源 | 汉字含义 | 点评`
- 固化表（09）：`译名 | 来源 | 本可意译 | 为何翻案无望`
- 典范表（07）：`中文译名 | 原词 / 来源 | 译者 / 出处 | 点评`

## 5. 入库流程（LLM 执行步骤）

1. 收到候选词条 → web 考证词源（语言、人名、历史译名），记录来源
2. 判据三问 → 按 §3 定类
3. 按 §4 格式写入对应文件
4. 更新 README「目录」计数与「领域速览」代表性词条
5. 提交信息格式：`feat: 新增 N 条（分类）` 或 `refine: 变更说明`

## 6. 红线

- **禁止编造词源**；拿不准的先开 Issue 或询问用户
- 巧译/造字（拓扑、泵、幽默）不得当作懒译收录
- 固化音译（沙发、雷达）不给"正确译文"——只记录为何翻案无望
- 食品饮料类文化词默认不收（咖啡、巧克力已在 09 作为"音译即惯例"特例）
- 品牌商标名不作为普通词条（可口可乐这类音译兼意译经典除外）
- 修改后必须自检：表格列数一致、备注一句话、README 计数与文件实际行数一致
